{% extends 'base.html.twig' %}

{% block title %}MascotasAnimales{% endblock %}

{% block body %}
    <h1>MascotasAnimales</h1>

    <table id=”tablaShow” class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ mascotas_animale.id }}</td>
            </tr>
            <tr>
                <th>Perros</th>
                <td>{{ mascotas_animale.perros }}</td>
            </tr>
            <tr>
                <th>Gatos</th>
                <td>{{ mascotas_animale.gatos }}</td>
            </tr>
            <tr>
                <th>Aves</th>
                <td>{{ mascotas_animale.aves }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_animales_index') }}">back to list</a>

    <a href="{{ path('app_animales_edit', {'id': mascotas_animale.id}) }}">edit</a>

    {{ include('animales/_delete_form.html.twig') }}
{% endblock %}
